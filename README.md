# aff_front_end
Front-end application for aff. With NextJS, typescript and tailwind, Fabric.js, threeJS

Each page should have a new branch. The branch name should be named in the format; <ft-pagename>.